node_modules/
dist/
dist-ssr/
*.local
.DS_Store
.env
.env.*
!.env.example

# Build/test noise
*.tsbuildinfo
coverage/

# bun.lock IS committed on purpose — CI uses `bun install --frozen-lockfile`.

# Local bunqueue server data (created by the control agent)
data/
